context(N);

for i in range(0, N) {
  for j in range(0, N) {
    x1[i] = (x1[i] + (a[i][j] * y_1[j]));
  }
}
for i in range(0, N) {
  for j in range(0, N) {
    x2[i] = (x2[i] + (a[j][i] * y_2[j]));
  }
}
